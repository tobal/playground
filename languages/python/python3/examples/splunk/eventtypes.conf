[logon]
search = TaskCategory=Logon OR (session opened)

[logoff]
search = TaskCategory=Logoff OR ("was logged off") OR ("initiated logoff") OR ("session closed")

[windowssession]
search = (TaskCategory=Logon OR TaskCategory=Logoff) AND (Logon_Type=10 OR Logon_Type=2)
